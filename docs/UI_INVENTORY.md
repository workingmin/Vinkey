# Vinkey UI 现状盘点

- 盘点日期：2026-09-02
- 依据：`src/App.tsx`、`src/components/*`、`src/store.ts`、`src/styles.css`
- 目的：为 UI 精细化调整提供可追踪的现状基线

## 0. 统计结论

- **8 类 UI 界面/表面**：Windows 标题栏、macOS 原生窗口区、项目与会话侧栏、对话页、文件列表页、文件编辑页、设置页、全局反馈表面。
- **5 个交互域**：应用壳层、项目与文件、AI 对话、文档编辑、设置与反馈。
- **14 个源码组件**：`App`、`TitleBar`、`IconButton`、`ContentPanel`、`ProjectSessionSidebar`、`FileBrowserPanel`、`WorkspaceTree`、`TreeItem`、`FileWorkspace`、`EditorPanel`、`CodeEditor`、`ChatPanel`、`ChatMessageItem`、`SettingsPage`。
- **3 类跨域状态**：本机工作区/会话状态、文档编辑状态、模型连接与流式生成状态。

## 1. 界面总览

| 界面/表面 | 入口 | 当前职责 | 实现状态 |
| --- | --- | --- | --- |
| Windows 窗口标题栏 | 应用根部 | 品牌、工作区/模型摘要、应用菜单、最小化/最大化/关闭 | 已实现 |
| macOS 原生菜单与 Overlay 标题区 | 桌面端 macOS | 系统菜单、交通灯和窗口行为 | 已实现 |
| 项目与会话侧栏 | 主窗口左侧 | 打开/切换工作区、刷新、搜索、新建/恢复会话、折叠、设置 | 已实现 |
| 对话页 | 内容区“对话”标签 | 消息流、复制、流式生成、停止、模型选择、上下文、发送 | 已实现 |
| 文件页（列表态） | 内容区“文件”标签 | 工作区文件树、筛选、新建文档/文件夹、刷新 | 已实现 |
| 文件页（编辑态） | 文件树点击文档 | 文档标签、编辑/分栏/预览、保存、收起编辑器、状态栏 | 已实现 |
| 设置页 | 侧栏底部“模型与应用设置” | 模型配置、连接测试、删除、主题切换 | 部分实现 |
| 错误/确认/输入反馈 | 操作失败或命令触发 | 错误条、`alert`、`prompt`、`confirm` | 已实现但待统一 |

## 2. 模块与组件地图

### 应用壳层

| 组件 | 文件 | 关键交互 |
| --- | --- | --- |
| `App` | `src/App.tsx` | 页面切换、工作区恢复、模型/会话加载、文档打开保存、快捷键、平台菜单 |
| `TitleBar` | `src/App.tsx` | 文件/编辑/查看/窗口/帮助菜单，窗口控制，主题切换 |
| `IconButton` | `src/App.tsx` | 统一图标按钮、tooltip、禁用和选中态 |
| `ContentPanel` | `src/App.tsx` | 对话/文件 tab、会话摘要、工作区与当前模型摘要 |

### 项目与文件

| 组件 | 文件 | 关键交互 |
| --- | --- | --- |
| `ProjectSessionSidebar` | `src/App.tsx` | 工作区入口、刷新、搜索会话/正文、会话选择、新建会话、折叠、设置 |
| `FileBrowserPanel` | `src/App.tsx` | 文件筛选、文件树动作、新建文档/文件夹、刷新、打开文档、添加上下文 |
| `WorkspaceTree` / `TreeItem` | `src/components/WorkspaceTree.tsx` | 目录展开收起、文件选中、左键打开、右键添加上下文 |
| `FileWorkspace` | `src/App.tsx` | 列表态和列表+编辑器双栏布局 |
| `EditorPanel` | `src/App.tsx` | 文档 tab、保存、视图切换、编辑器收起、状态统计 |
| `CodeEditor` | `src/components/CodeEditor.tsx` | CodeMirror 文本编辑、Markdown 语法、换行、撤销/重做、主题 |

### AI 对话

| 组件 | 文件 | 关键交互 |
| --- | --- | --- |
| `ChatPanel` | `src/App.tsx` | 输入、Enter/Shift+Enter、上下文预算、模型切换、发送/停止、流式响应持久化 |
| `ChatMessageItem` | `src/App.tsx` | Markdown 渲染、助手/用户布局、复制、时间、生成中状态 |

### 设置

| 组件 | 文件 | 关键交互 |
| --- | --- | --- |
| `SettingsPage` | `src/components/SettingsPage.tsx` | 模型/外观分组、配置增删改、连接测试、API Key 处理、Esc 返回、主题 |

## 3. 当前可见交互清单

### 导航与窗口

- Windows 自绘标题栏双击切换最大化；按钮执行最小化、最大化/还原、关闭。
- macOS 使用原生全局菜单；菜单命令与 Windows 保持同一集合。
- 顶部“对话/文件”是互斥页面切换；进入“文件”时默认收起编辑器。
- 设置打开时自动折叠侧栏；关闭时恢复进入前状态，设置期间手动切换优先。

### 工作区与搜索

- 打开项目后显示名称和路径；可刷新或切换项目。
- 侧栏搜索同时匹配会话标题和文档正文，文档结果显示路径、行号和摘要。
- 文件页支持名称筛选；目录可展开/收起；文件左键打开，右键切换对话上下文。
- 新建文档和文件夹通过路径输入完成；新建文档后自动打开编辑器。

### 对话

- 没有模型时发送操作转入设置；有模型时按上下文预算决定是否允许发送。
- Enter 发送，Shift+Enter 换行，中文 IME 组字期间不发送。
- 生成时发送按钮变为停止；文本按 chunk 追加到同一条助手消息。
- 每条有内容的消息支持复制；失败显示全局错误条。

### 文档

- Markdown 支持编辑/分栏/预览；TXT 仅编辑。
- 修改内容后显示未保存标记；保存仅更新原位状态。
- 状态栏显示行数、字符数、词数、编码、换行符和保存状态。
- `Ctrl/Cmd+S` 和标题栏“保存文档”都保存当前活动文档。

### 设置

- 模型类型为 Ollama 或 OpenAI 兼容；可编辑名称、Base URL、模型、上下文窗口。
- OpenAI 兼容配置可输入或删除 API Key；凭据不写入 SQLite、前端存储或日志。
- 可测试连接、读取可用模型、保存并启用或删除配置。
- 外观目前只有深色/浅色主题。

## 4. 状态与数据边界

| 状态 | 目前表现 | 目标态缺口 |
| --- | --- | --- |
| 未打开工作区 | 侧栏/文件页显示打开项目或打开文件夹 | 最近工作区入口需明确化 |
| 未配置模型 | 输入区显示“添加模型” | 对话页可提供更明确的引导 |
| 搜索中 | 侧栏显示正在搜索文档 | 需要加载态尺寸稳定验收 |
| 上下文超限 | 发送被阻止，错误条提示移除/缩短 | 应标出超额文件并提供逐项移除 |
| 未保存 | 标签点、状态栏“未保存”、保存按钮可用 | 外部冲突与放弃修改确认待实现 |
| 连接失败 | 设置页内联连接结果或全局错误条 | 统一重试和错误文案 |
| AI 修改提案 | 当前没有 diff 审核 UI | 待实现接受/拒绝/放弃流程 |

## 5. 设计稿与实现的主要差异

1. 设计稿设想的 AI 修改 diff 审核、外部文件冲突条和 `@` 文件引用尚未进入当前实现。
2. 设计稿列出的编辑器、数据、权限等设置分组尚未实现，目前只有“模型”和“外观”。
3. 设计稿建议的 toast、原生确认对话框和统一内联重试还未形成统一组件，当前仍混用 `alert`、`prompt`、`confirm` 和错误条。
4. `EditorPanel` 目前数据模型只有单活动文档，虽保留 `tabs` 状态，但没有多标签切换 UI。

后续精调应先选择“现状优化”或“目标态补齐”，并在对应设计域文档中记录验收条件。
