# Vinkey UI 现状盘点

- 盘点日期：2026-09-15
- 对比基线：2026-09-02 盘点；重点核对 2026-09-03 至 `a5e882b` 的实现提交
- 依据：`src/App.tsx`、`src/components/*`、`src/store.ts`、`src/styles.css`、`src/lib/desktop.ts`、[UI_ENTRY_POINTS.md](./UI_ENTRY_POINTS.md)
- 目的：为 UI 精细化调整提供可追踪的现状基线

## 0. 统计结论

- **13 类 UI 界面/表面**：Windows 标题栏、macOS 原生窗口区、项目与会话侧栏、对话页、文件列表页、文件编辑页、任务中心、设置页、AI 修改提案条、分析产物预览、删除确认、运行日志、全局错误/状态反馈。
- **6 个交互域**：应用壳层、项目与文件、AI 对话、文档编辑、后台任务、设置与反馈。
- **21 个源码组件/组件函数**：`App`、`TitleBar`、`IconButton`、`ContentPanel`、`ProjectSessionSidebar`、`FileBrowserPanel`、`WorkspaceTree`、`TreeItem`、`FileWorkspace`、`EditorPanel`、`CodeEditor`、`ChatPanel`、`ChatMessageItem`、`MarkdownContent`、`MessageActivity`、`TaskCenter`、`SettingsPage`、`ModelPicker`、`FilePreview`、`RecordDeletionDialog`、`CodeBlock`。
- **5 类跨域状态**：本机工作区/多项目与会话状态、文档编辑与修改提案、模型连接/活动模型状态、流式对话/分析活动状态、可恢复后台任务状态。

## 1. 界面总览

| 界面/表面 | 入口 | 当前职责 | 实现状态 |
| --- | --- | --- | --- |
| Windows 窗口标题栏 | 应用根部 | 品牌、工作区/模型摘要、应用菜单、最小化/最大化/关闭 | 已实现 |
| macOS 原生菜单与 Overlay 标题区 | 桌面端 macOS | 系统菜单、交通灯和窗口行为 | 已实现 |
| 项目与会话侧栏 | 主窗口左侧 | 打开/切换工作区、刷新、搜索、新建/恢复会话、折叠、设置 | 已实现 |
| 对话页 | 内容区“对话”标签 | 消息流、复制、流式生成、停止、活动模型标识、上下文、发送 | 已实现 |
| 文件页（列表态） | 内容区“文件”标签 | 工作区文件树、筛选、新建文档/文件夹、刷新 | 已实现 |
| 文件页（编辑态） | 文件树点击文档 | 文档标签、编辑/分栏/预览、保存、收起编辑器、状态栏 | 已实现 |
| 任务中心 | 内容区“任务”标签、标题栏“任务中心” | 后台任务状态、步骤、失败重试、结果查看 | 已实现 |
| 设置页 | 侧栏底部“模型与应用设置” | 当前模型、模型服务、准入检查、硬件档位、主题切换 | 模型域已实现，更多设置分组待实现 |
| AI 修改提案条 | 对话页/文件编辑页 | 查看待审核提案、接受/拒绝、应用后保存 | 已实现（基础单块/多块提案） |
| 分析产物预览 | 对话消息活动或任务中心结果 | 查看长文本分析产物及来源校验状态 | 已实现 |
| 删除确认对话框 | 项目或会话删除命令 | 二次确认、输入确认词、失败重试 | 已实现 |
| 运行日志面板 | 标题栏“窗口/帮助”诊断命令 | 查看、刷新、复制跨平台运行事件 | 已实现 |
| 错误/确认/输入反馈 | 操作失败或命令触发 | 错误条、内联状态、原生 `alert`/`prompt`/`confirm` | 已实现但待统一 |

## 2. 模块与组件地图

### 应用壳层

| 组件 | 文件 | 关键交互 |
| --- | --- | --- |
| `App` | `src/App.tsx` | 页面切换、工作区恢复、模型/会话加载、文档打开保存、快捷键、平台菜单 |
| `TitleBar` | `src/App.tsx` | 文件/编辑/查看/窗口/帮助菜单，窗口控制，主题切换 |
| `IconButton` | `src/App.tsx` | 统一图标按钮、tooltip、禁用和选中态 |
| `ContentPanel` | `src/App.tsx` | 对话/文件 tab、会话摘要、工作区与当前模型摘要 |

### 项目与文件

| 组件 | 文件 | 关键交互 |
| --- | --- | --- |
| `ProjectSessionSidebar` | `src/components/ProjectSessionSidebar.tsx` | 工作区入口、刷新、搜索会话/正文、会话选择、新建会话、折叠、设置 |
| `FileBrowserPanel` | `src/App.tsx` | 文件筛选、文件树动作、新建文档/文件夹、刷新、打开文档、添加上下文 |
| `WorkspaceTree` / `TreeItem` | `src/components/WorkspaceTree.tsx` | 目录展开收起、文件选中、左键打开、右键添加上下文 |
| `FileWorkspace` | `src/App.tsx` | 列表态和列表+编辑器双栏布局 |
| `EditorPanel` | `src/App.tsx` | 文档 tab、保存、视图切换、编辑器收起、状态统计 |
| `CodeEditor` | `src/components/CodeEditor.tsx` | CodeMirror 文本编辑、Markdown 语法、换行、撤销/重做、主题 |

### AI 对话

| 组件 | 文件 | 关键交互 |
| --- | --- | --- |
| `ChatPanel` | `src/App.tsx` | 输入、Enter/Shift+Enter、上下文预算、活动模型标识、发送/停止、流式响应持久化 |
| `ChatMessageItem` | `src/App.tsx` | Markdown 渲染、助手/用户布局、复制、时间、生成中状态 |
| `MarkdownContent` | `src/components/MarkdownContent.tsx` | 安全 Markdown/代码块渲染、分析结果正文展示 |
| `MessageActivity` | `src/components/MessageActivity.tsx` | 折叠/展开任务活动轨迹、产物预览入口 |

### 设置

| 组件 | 文件 | 关键交互 |
| --- | --- | --- |
| `SettingsPage` | `src/components/SettingsPage.tsx` | 唯一功能模型、连接增删改、准入探测、API Key 处理、硬件信息、Esc 返回 |
| `ModelPicker` | `src/components/SettingsPage.tsx` | 当前模型选择、键盘导航、上下展开、检查状态 |

### 后台任务与反馈

| 组件 | 文件 | 关键交互 |
| --- | --- | --- |
| `TaskCenter` | `src/components/TaskCenter.tsx` | 任务计数、步骤展开、失败步骤选择、重试、结果查看 |
| `RecordDeletionDialog` | `src/components/RecordDeletionDialog.tsx` | 项目/会话记录删除确认、错误留在对话框内重试 |
| 运行日志面板 | `src/App.tsx` | 运行事件、平台/版本元数据、复制和刷新 |

## 3. 当前可见交互清单

跨页面业务功能入口、前置条件和跳转目标统一见 [UI_ENTRY_POINTS.md](./UI_ENTRY_POINTS.md)；本节只保留当前实现的可见交互事实。

### 导航与窗口

- Windows 自绘标题栏双击切换最大化；按钮执行最小化、最大化/还原、关闭。
- macOS 使用原生全局菜单；菜单命令与 Windows 保持同一集合。
- 顶部“对话/文件”是互斥页面切换；进入“文件”时默认收起编辑器。
- 设置打开时自动折叠侧栏；关闭时恢复进入前状态，设置期间手动切换优先。
- 侧栏支持多项目展开/切换，项目和会话删除均经过确认对话框；主题可在侧栏和应用菜单切换。
- macOS 原生窗口按钮会根据侧栏宽度同步布局；窄窗口下设置页和标题栏有专用响应式规则。

### 工作区与搜索

- 打开项目后显示名称和路径；可刷新或切换项目。
- 侧栏搜索同时匹配会话标题和文档正文，文档结果显示路径、行号和摘要。
- 文件页支持名称筛选；目录可展开/收起；文件左键打开，右键切换对话上下文。
- 新建文档和文件夹通过路径输入完成；新建文档后自动打开编辑器。

### 对话

- 没有模型时发送操作转入设置；有模型时按上下文预算决定是否允许发送。
- Enter 发送，Shift+Enter 换行，中文 IME 组字期间不发送。
- 生成时发送按钮变为停止；文本按 chunk 追加到同一条助手消息。
- 每条有内容的消息支持复制；失败显示全局错误条。
- 输入 `@` 后可按文件名/相对路径筛选，使用方向键、Enter/Tab 将文件加入上下文。
- 已引用文件显示为可移除标签；文档分析、人物线、项目分析提供快捷入口。
- 长任务显示可折叠活动轨迹，包含读取、分析、证据校验和产物路径等状态。

### 文档

- Markdown 支持编辑/分栏/预览；TXT 仅编辑。
- 修改内容后显示未保存标记；保存仅更新原位状态。
- 状态栏显示行数、字符数、词数、编码、换行符和保存状态。
- `Ctrl/Cmd+S` 和标题栏“保存文档”都保存当前活动文档。
- AI 改稿结果在文件页显示修改提案条，支持接受或拒绝；接受后仍保留未保存状态。
- 图片、PDF、音视频和二进制文件进入预览/下载路径；Markdown 预览使用安全渲染。

### 任务中心

- “任务”页显示运行、完成和失败计数，按任务展开步骤与检查点。
- 失败任务可选择步骤重跑；不可重试失败明确提示需重新发起任务。
- 已完成任务可打开分析产物，显示模型调用、缓存命中、耗时和产物路径。

### 设置

- 模型类型为 Ollama 或 OpenAI 兼容；当前模型为应用级唯一活动模型。
- 服务支持发现模型、顺序准入检查、检查结果缓存、上下文自动配置和硬件档位提示。
- 连接按规范化服务地址 + API Key 指纹去重；同地址不同密钥可共存，名称不参与去重。
- API Key 不写入 SQLite、浏览器持久化存储或日志；桌面端使用系统凭据库，浏览器模式仅保存不可逆指纹。
- 模型选择器支持键盘操作、根据可用空间向上/向下展开，并保留“需要检查”的已保存当前模型。
- 外观目前只有深色/浅色主题。

## 4. 状态与数据边界

| 状态 | 目前表现 | 目标态缺口 |
| --- | --- | --- |
| 未打开工作区 | 侧栏/文件页显示打开项目或打开文件夹 | 最近工作区入口需明确化 |
| 未配置模型 | 输入区显示“添加模型” | 对话页可提供更明确的引导 |
| 搜索中 | 侧栏显示正在搜索文档 | 需要加载态尺寸稳定验收 |
| 上下文超限 | 发送被阻止，错误条提示移除/缩短 | 应标出超额文件并提供逐项移除 |
| 未保存 | 标签点、状态栏“未保存”、保存按钮可用 | 外部冲突与统一放弃修改确认待实现 |
| 连接失败 | 设置页内联连接结果、模型列表错误和全局错误条 | 统一重试和错误文案 |
| AI 修改提案 | 对话页提示，文件编辑页显示提案条，可接受/拒绝 | 多块批量审核、放弃全部和更完整 diff 视图待实现 |
| 分析活动 | 消息内折叠轨迹、任务中心显示步骤和检查点 | 完整 Runtime 事件模型和统一暂停/恢复交互待实现 |
| 后台任务失败 | 任务中心显示失败原因、可重试性和步骤重跑 | 任务通知和跨会话跳转待实现 |
| 项目/会话删除 | 原生确认对话框，项目删除要求输入确认词 | 统一对话框组件待实现 |
| 模型连接重复 | 保存前校验，数据库唯一索引，历史重复迁移 | UI 中尚未提供“已存在连接”专用冲突定位 |

## 5. 设计稿与实现的主要差异

1. `@` 文件引用和基础 AI 修改提案审核已经实现，但 `UI_DESIGN.md`、`UI_DESIGN_CHAT.md`、`UI_DESIGN_STATES.md` 仍把它们标为目标态，需同步文档状态。
2. 当前提案审核是编辑器内联基础视图，设计稿要求的完整 diff、接受全部/逐块审核、放弃提案流程仍未完全实现。
3. 设计稿列出的编辑器、数据、权限等设置分组尚未实现，目前设置页聚焦模型服务和外观主题。
4. 设计稿建议的 toast、统一原生确认对话框和统一内联重试还未形成公共组件，当前仍混用 `alert`、`prompt`、`confirm`、错误条和局部对话框。
5. `EditorPanel` 目前数据模型保留 `tabs`，但 UI 仍没有多标签切换；外部文件冲突、对比、重新加载和另存为也未实现。

## 6. 2026-09-02 后 UI 变更核对

| 日期 | 已确认的 UI 变化 | 当前验收状态 |
| --- | --- | --- |
| 2026-09-03 | `@` 文件引用、对话交互状态、可折叠活动轨迹、文档分析快捷入口、跨平台运行日志 | 已实现；设计文档状态标记需补同步 |
| 2026-09-04 | 会话侧栏操作、聚焦/概览/深度分析入口、结构化本地文学任务反馈 | 已实现；完整 Agent Runtime 仍为目标态 |
| 2026-09-07 | 后台任务中心、任务步骤、失败重跑和结果入口 | 已实现；暂停/恢复和通知仍待补齐 |
| 2026-09-08 | 多项目侧栏、项目/会话记录删除确认、侧栏主题切换、macOS 窗口控制适配 | 已实现 |
| 2026-09-09 至 10 | 硬件感知模型提示、结构化错误、长文本活动/产物展示、Markdown 安全内容组件 | 已实现；视觉跨平台截图验收仍需执行 |
| 2026-09-14 | 设置页重构、单一活动模型、服务列表/表单、模型目录操作、准入结果缓存、连接状态操作布局 | 已实现 |
| 2026-09-15 | 当前模型选择器键盘/空间定位、上下文自动配置、连接复合身份去重及重复错误反馈 | 已实现；Rust 桌面构建需在目标平台复验 |

本盘点以当前源码为准；后续 UI 修改应先更新本文件的页面清单和变更核对表，再同步对应域设计文档中的“已实现/部分实现/待实现”标记。

后续精调应先选择“现状优化”或“目标态补齐”，并在对应设计域文档中记录验收条件。
