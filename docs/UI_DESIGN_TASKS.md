# UI 设计：任务中心

- 状态：当前实现基线，任务可观测性扩展为目标态
- 日期：2026-09-16
- 适用端：Windows、macOS 桌面应用；浏览器模式仅提供有限演示能力
- 关联文档：[UI_INVENTORY.md](./UI_INVENTORY.md)、[UI_DESIGN_CHAT.md](./UI_DESIGN_CHAT.md)、[UI_DESIGN_STATES.md](./UI_DESIGN_STATES.md)、[TEMPORARY_INTERACTION_STATES.md](./TEMPORARY_INTERACTION_STATES.md)

## 目标与边界

任务中心是当前工作区长文本分析和 Worker 任务的只读可观测性页面，用于查看运行状态、结果与公开业务链路，辅助定位任务在哪个阶段失败或停滞。它不承担普通对话消息流，也不提供通用 IDE 的构建、终端或插件任务入口。

当前页面主要服务长文本分析和 Worker 任务：

- 查看运行、暂停、完成、失败和取消任务。
- 查看每个任务的步骤、执行次数、检查点和失败原因。
- 查看已完成任务的只读分析产物和运行指标。
- 查看公开事件时间线并复制不含正文和凭据的诊断摘要。
- 返回任务来源会话，在来源上下文中暂停、继续、取消或重试任务。
- 以当前工作区为边界刷新任务列表，避免跨项目混淆。

任务中心不改变任务状态，不直接编辑源文档、不修改任务输入、不替代文件页的 Diff 提案审核。暂停、继续、取消和失败重试统一由来源会话承担；任务产生的草稿或修改提案仍回到对话/文件页处理。

## 页面入口

- 内容区顶栏的“任务”标签与“对话”“文件”互斥。
- 标题栏“查看”菜单提供“任务中心”命令；命令只切换内容页，不改变当前会话。
- 无工作区时仍可进入页面，但显示“未打开项目”空态，不请求任务列表。
- 设置页打开时任务中心不可见；任务执行期间设置页会锁定模型和连接修改。

## 信息归属

- 项目表示任务列表的查询范围。当前任务中心只读取当前项目，因此顶部显示“范围：当前项目 · 项目名”，任务行不重复项目名。
- 当前活动模型不代表历史任务实际使用的模型。任务中心顶部不把“当前项目 · 当前模型”作为任务归属；如应用壳层必须保留活动模型，应明确标注为“新任务默认模型”。
- 任务实际使用的模型属于执行快照，在任务展开详情中显示模型名和服务名；切换活动模型不能改变已有任务的记录。
- 会话表示任务来源，放在任务标题的次级信息中。无会话来源的后台任务显示“独立任务”。
- 跨项目汇总属于目标态。后端支持全局任务索引后，顶部增加“全部项目/当前项目”范围选择；只有“全部项目”范围下才在任务行显示项目短名。

## 页面预览（Markdown 预览）

以下 HTML 展示目标布局，可在 Markdown Preview 中预览；它只用于设计验收，不承载交互逻辑。与当前代码的差异在“当前实现对照”和组件映射中明确标记。

<table border="1" cellpadding="8" cellspacing="0" width="100%">
  <tr>
    <td colspan="4" bgcolor="#F1F3F5"><strong>任务中心</strong>　范围：当前项目 · 风雪夜归人　　　　　　　<strong>对话</strong>　|　<strong>文件</strong>　|　<strong>任务</strong></td>
  </tr>
  <tr>
    <td colspan="3" bgcolor="#F8F9FA"><strong>运行</strong>　3　　<strong>完成</strong>　12　　<strong>失败</strong>　1</td>
    <td width="16%" bgcolor="#F8F9FA" align="right"><strong>刷新</strong></td>
  </tr>
  <tr bgcolor="#F1F3F5">
    <td width="42%"><strong>任务</strong></td>
    <td width="16%"><strong>状态</strong></td>
    <td width="18%"><strong>更新时间</strong></td>
    <td><strong>查看</strong></td>
  </tr>
  <tr>
    <td>▾ 分析《第三卷》人物关系<br><small>会话：人物线梳理 · #7F3A</small></td>
    <td><strong>运行中</strong></td>
    <td>刚刚</td>
    <td>来源　复制诊断</td>
  </tr>
  <tr>
    <td colspan="4" bgcolor="#FFFFFF">
      <strong>步骤与检查点</strong>　文档分块 ✓　逐块分析 <strong>进行中</strong>　章节汇总 …<br>
      <small>执行模型：qwen3:8b · 本地 Ollama　·　已完成 4/12　·　检查点 cp-04</small>
    </td>
  </tr>
  <tr>
    <td>▾ 梳理长篇章节结构<br><small>会话：全书结构讨论 · #6A21</small></td>
    <td><strong>失败</strong></td>
    <td>5 分钟前</td>
    <td>来源　复制诊断</td>
  </tr>
  <tr>
    <td colspan="4" bgcolor="#FFF8E8">
      <strong>错误 E-TASK-RETRY</strong>　章节汇总失败，支持从“章节汇总”重新执行<br>
      <small>业务链路：任务启动 → 文档分块 → 逐块分析 → 章节汇总失败</small>
    </td>
  </tr>
  <tr>
    <td>▸ 汇总全项目故事线<br><small>会话：项目概览 · #4C90</small></td>
    <td><strong>已完成</strong></td>
    <td>昨天 18:42</td>
    <td>结果　来源　复制诊断</td>
  </tr>
  <tr>
    <td colspan="4" bgcolor="#F4F5F6">
      <strong>分析产物（只读）</strong>　产物目录：analysis/2026-09-15　·　耗时 08:32　·　模型调用 12 次　·　缓存命中 8 次<br>
      <small>结果正文在固定区域滚动，不直接写回源文档</small>
    </td>
  </tr>
</table>

### 工具栏

- 工具栏保持稳定高度；左侧按“运行”统计 `running + paused`，分别统计“完成”和“失败”。
- 右侧只有刷新按钮。刷新期间按钮禁用并显示旋转图标，列表保留原内容，避免跳变。
- 统计只针对当前工作区，由 `listTaskJobs()` 返回结果计算。
- 项目范围位于页面顶部，不与统计混排；当前实现阶段仅展示当前项目，不提供无效的跨项目选择器。

### 任务行

- 列表提供可见表头，并严格使用“任务 / 状态 / 更新时间 / 查看”四列；所有任务行复用相同列宽。
- 默认每个任务折叠。任务列使用两级信息：第一行显示可读任务摘要，第二行显示会话摘要和 4–6 位短任务编号。
- 点击任务主按钮展开/收起详情；当前实现保持单个展开。
- 展开按钮使用 `aria-expanded`；项目名、会话名和任务摘要通过独立字段截断，tooltip 展示完整值，不能拼成长字符串后整体截断。
- 查看列只提供只读或导航能力：完成任务可查看结果；有来源的任务可返回来源会话；所有任务可复制诊断摘要。
- 页面不得调用暂停、恢复、取消或重跑接口，不提供步骤选择器。任务状态变更统一在来源会话内完成。

### 任务标题与来源

- 不使用“项目名 · 会话名 · 任务名”的单行三段式标题，避免长项目名和会话名挤掉任务含义。
- 主标题使用任务动作和对象生成确定性摘要，例如“分析《第三卷》人物关系”“汇总全项目故事线”，建议控制在 16–24 个中文字符。
- 次标题使用“会话：会话摘要 · #短编号”；无会话来源时使用“独立任务 · #短编号”。短编号只用于消歧，不能代替可读任务摘要。
- 当前项目范围下不在每行重复项目名；未来“全部项目”范围下，在主标题前增加项目短名标签。
- 完整项目名、会话名、任务 ID、来源消息和创建时间进入 tooltip 或展开详情。
- 任务摘要在创建时生成并持久化。首版采用本地确定性规则，不依赖额外模型调用；会话后续改名不覆盖任务创建时的来源快照。

### 任务详情

- 失败任务优先显示错误代码、用户可读错误消息和“可重试/需重新发起”语义。
- 步骤使用中文短标签：文档分块、逐块分析、章节汇总、卷级汇总、最终综合、证据校验和分层归并。
- 每步显示状态、执行次数和检查点；状态不能只依赖颜色，应同时使用文字、图标或位置。
- 详情显示任务创建时的模型名和服务名快照；模型后来切换、重命名或删除时，历史任务仍保留原执行信息。
- 详情按时间显示公开业务事件，至少包含时间、事件语义和关联步骤；不展示隐藏思维链、请求正文或事件中的敏感字段。
- 完成结果显示模型调用次数、跨任务缓存命中、任务内检查点命中、耗时、产物目录和结果正文。
- 产物正文保持只读，长内容在固定区域滚动，不把结果直接写回源文档。

## 状态语义

### 任务状态

| 稳定状态 | 用户文案 | 表现 | 当前实现 |
| --- | --- | --- | --- |
| `planned` | 等待中 | 中性文字，允许展开查看已规划步骤 | 已实现 |
| `running` | 运行中 | 强调色文字，步骤中显示当前执行项 | 已实现 |
| `paused` | 已暂停 | 警示色文字，计入运行统计 | 已实现（展示） |
| `completed` | 已完成 | 成功色文字，可查看结果 | 已实现 |
| `failed` | 失败 | 错误色文字，显示失败原因和来源会话入口 | 已实现 |
| `cancelled` | 已取消 | 中性终止状态，不提供结果查看 | 已实现（展示） |

### 步骤状态

`pending`、`running`、`completed`、`failed`、`cancelled` 只描述当前步骤，不覆盖任务级失败原因。步骤状态、执行次数和检查点来自持久化 `TaskJob`，前端不根据等待时间猜测状态。

### 与消息活动轨迹的边界

- 消息活动轨迹位于对话消息内部，回答“这次请求正在做什么”。
- 任务中心回答“工作区有哪些后台任务、运行到哪里、哪里失败、结果在哪里”。
- 来源会话回答“如何暂停、继续、取消或重试这次任务”。
- 两者可以引用同一个 `jobId`，但任务中心不重复渲染隐藏思维链或逐 token 输出。
- 任务完成后消息流突出最终回答，任务中心保留可恢复任务和产物索引。

## 核心流程

### 查看任务

1. 用户进入“任务”页。
2. 页面按当前工作区读取任务，并按 `updatedAt` 倒序排列。
3. 用户展开任务查看步骤、检查点和错误。
4. 用户点击刷新时保留已展开状态；任务不存在或工作区切换后清理失效详情。

### 返回来源处理

1. 有 `conversationId` 的任务显示“返回来源会话”入口；旧任务缺少来源字段时不伪造入口。
2. 入口加载任务所属的当前项目会话并切换到对话页，不改变任务状态。
3. 来源会话按持久化 `TaskJob` 显示运行、暂停或失败任务的控制区。
4. 运行中任务可暂停或取消，暂停任务可继续或取消，可重试失败可选择检查点后重试。
5. 不可重试失败保留原因，但不能提交重试。

控制命令仍由后端保证状态转换、幂等与检查点有效性；任务中心只负责定位和导航。

### 查看业务链路与复制诊断

1. 展开任务后按时间显示最近 50 条公开任务事件。
2. 事件显示用户可理解的语义，并保留步骤名称用于定位链路阶段。
3. 复制诊断包含任务 ID、状态、项目/会话/模型快照、失败信息和公开事件。
4. 诊断摘要不得包含 API Key、请求正文、源文件指纹或隐藏思维链。

### 查看结果

1. 只有完成任务显示查看结果入口。
2. 首次查看调用 `getLongTextWorkerOutput(jobId)`，结果加载期间保留任务详情。
3. 结果显示只读正文、产物目录和运行指标；读取失败使用全局错误反馈。
4. 结果已加载时再次点击只切换详情展开状态，不重复请求。

## 空态、加载和错误

| 状态 | 设计表现 | 当前实现 |
| --- | --- | --- |
| 未打开工作区 | 图标 + “未打开项目”，不触发后台请求 | 已实现 |
| 无后台任务 | 图标 + “暂无后台任务”，占满可用内容高度 | 已实现 |
| 首次加载 | 列表区域显示稳定的读取状态，工具栏刷新禁用 | 部分实现，当前仅刷新按钮进入加载态 |
| 定时刷新 | 每 2.5 秒刷新一次；保留当前页面结构 | 已实现 |
| 列表读取失败 | 使用全局错误条，下一次定时刷新仍可恢复 | 已实现 |
| 结果读取失败 | 保留任务行和详情，错误不覆盖任务状态 | 部分实现，当前错误进入全局错误条 |
| 任务已失效 | 显示“任务结果尚未完成或已经失效” | 已实现 |

## 响应式与无障碍

- 桌面宽度任务行采用任务、状态、时间、查看四列；内容区最小宽度不足时任务行改为两列，时间隐藏，查看入口移到下一行。
- 任务详情和结果正文允许纵向滚动；长 task ID、错误消息、路径和结果文本自动换行。
- 所有图标按钮必须有可读 `title` 和 `aria-label`；展开按钮提供 `aria-expanded`。
- 刷新和结果读取期间避免重复请求；状态变化应使用状态文本而不是仅改变颜色。来源会话的控制命令提交期间按钮禁用。
- 深浅主题、键盘焦点、中文 IME 和 `prefers-reduced-motion` 按系统级验收标准复验。

## 数据与隐私边界

- 任务列表只显示当前工作区任务；任务输入哈希、源文件指纹和内部事件不直接作为 UI 主文案。
- 结果正文和产物路径可能包含用户创作内容，任务中心不写入浏览器历史或日志。
- 产物预览为只读，不允许在任务中心直接覆盖源文件。
- API Key、模型凭据和隐藏思维链不进入任务列表、步骤详情或结果指标。

### 任务展示元数据（目标态）

当前 `TaskJob` 只有 `workspaceId`、`taskType` 和 `instructionHash` 等执行字段，不能可靠生成任务摘要、会话来源和实际模型。目标态在任务创建时增加以下持久化字段；旧任务缺少字段时使用任务类型、完整 task ID 和“来源未知”降级展示。

| 字段 | 用途 | 展示规则 |
| --- | --- | --- |
| `displayTitle` | 稳定、可读的任务摘要 | 主标题；本地确定性生成，可由后续功能显式重命名 |
| `conversationId` / `sourceMessageId` | 返回任务来源 | 不直接作为主文案；用于跳转和消歧 |
| `workspaceNameSnapshot` | 创建时项目名称 | 全部项目范围或详情展示；项目重命名后仍可追溯 |
| `conversationTitleSnapshot` | 创建时会话名称 | 次标题；后续会话改名不覆盖快照 |
| `modelProfileId` | 执行模型引用 | 用于内部关联，不单独作为历史显示依据 |
| `modelNameSnapshot` / `connectionNameSnapshot` | 实际模型和服务 | 展开详情展示；模型配置删除后仍保留历史语义 |

模型字段只保存名称和服务显示快照，不保存 API Key、请求正文或连接凭据。新增字段应提供序列化默认值，保证已有 `task.json` 可以继续读取。

## 当前实现对照

| 设计区域 | 实现位置 | 状态 |
| --- | --- | --- |
| 页面入口和内容页切换 | `src/App.tsx` | 已实现 |
| 顶部项目范围 | `src/App.tsx` | 已实现；任务页显示当前项目范围，不混入当前活动模型 |
| 四列表头和列对齐 | `src/components/TaskCenter.tsx`、`src/styles.css` | 已实现；使用“任务 / 状态 / 更新时间 / 查看”稳定列宽 |
| 任务摘要、会话来源和短编号 | `src/components/TaskCenter.tsx`、`TaskJob` | 已实现；旧任务按类型和短编号降级显示 |
| 任务统计、列表、展开详情 | `src/components/TaskCenter.tsx` | 已实现 |
| 任务刷新与 2.5 秒轮询 | `src/components/TaskCenter.tsx` | 已实现 |
| 失败原因与业务链路 | `src/components/TaskCenter.tsx` | 已实现；只读展示失败语义、步骤和公开事件时间线 |
| 完成结果和运行指标 | `src/components/TaskCenter.tsx` | 已实现 |
| 每任务执行模型快照 | `TaskJob`、`src/components/TaskCenter.tsx` | 已实现；新增字段兼容旧任务读取 |
| 消息内活动轨迹和产物对话框 | `src/components/MessageActivity.tsx` | 已实现，职责与任务中心分离 |
| 任务生命周期操作 | `src/components/ConversationTaskControls.tsx`、`src/lib/desktop.ts` | 已迁移到来源会话；任务中心不调用状态变更接口 |

## 组件与业务功能映射

本表是任务中心页面验收清单；对话页中的任务恢复、暂停和活动轨迹由 `UI_DESIGN_CHAT.md` 负责。

| 组件 ID | 类型 | 组件 | 功能点 / 入口 | 行为或结果 | 实现位置 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| `UI-TASK-COUNTS` | 状态 | 运行、完成、失败计数 | `BF-TASK-001` / `EP-TASK-001` | 汇总当前工作区任务状态 | `src/components/TaskCenter.tsx` | 已实现 |
| `UI-TASK-REFRESH` | 操作 | 刷新按钮和加载图标 | `BF-TASK-001` / `EP-TASK-001` | 重新读取任务列表；请求期间禁用 | `src/components/TaskCenter.tsx` | 已实现 |
| `UI-TASK-LIST-STATE` | 状态 | 未打开项目、暂无任务和全局读取错误 | `BF-TASK-001` / `EP-TASK-001` | 表达任务列表边界状态 | `src/components/TaskCenter.tsx`、`src/App.tsx` | 部分实现（首次加载和局部错误待补） |
| `UI-TASK-ROW-TOGGLE` | 操作/结果 | 任务摘要、来源次标题和展开/收起按钮 | `BF-TASK-002` / `EP-TASK-002` | 显示任务摘要、会话来源和短编号；保持单个任务详情展开并同步 `aria-expanded` | `src/components/TaskCenter.tsx` | 已实现 |
| `UI-TASK-STATUS` | 状态 | 任务状态、更新时间、步骤和执行元数据 | `BF-TASK-001`、`BF-TASK-002` / `EP-TASK-001`、`EP-TASK-002` | 展示持久化 Job/Step 状态、执行次数、检查点及模型快照 | `src/components/TaskCenter.tsx` | 已实现 |
| `UI-TASK-FAILURE` | 结果 | 错误代码、消息和可重试语义 | `BF-TASK-002`、`BF-TASK-003` / `EP-TASK-002`、`EP-TASK-003` | 区分可重试失败和需重新发起 | `src/components/TaskCenter.tsx` | 已实现 |
| `UI-TASK-OUTPUT` | 入口/结果 | 查看结果按钮和只读结果区 | `BF-TASK-004` / `EP-TASK-004` | 展示正文、产物目录、耗时和缓存/模型指标 | `src/components/TaskCenter.tsx` | 已实现 |
| `UI-TASK-EVENTS` | 结果 | 公开业务事件时间线 | `BF-TASK-002` / `EP-TASK-002` | 按时间展示任务生命周期和步骤事件，不显示隐藏思维链 | `src/components/TaskCenter.tsx` | 已实现 |
| `UI-TASK-DIAGNOSTICS` | 操作/结果 | 复制诊断信息 | `BF-TASK-002` / `EP-TASK-002` | 复制任务标识、状态、快照、失败信息和公开事件 | `src/components/TaskCenter.tsx` | 已实现 |
| `UI-TASK-SOURCE-LINK` | 入口 | 返回来源会话 | `BF-TASK-007` / `EP-TASK-007` | 加载稳定来源会话，由来源会话承接任务控制 | `src/components/TaskCenter.tsx`、`src/App.tsx` | 已实现 |

## 验收标准

1. 从内容区“任务”标签和标题栏菜单均可进入任务中心，且不改变当前会话或工作区。
2. 顶部明确显示任务范围；当前项目与当前活动模型不再以同一归属文案混排，活动模型不能被误认为历史任务模型。
3. 无工作区、无任务、加载中、读取失败和结果失效均有稳定且可理解的状态表现。
4. 列表具有可见的“任务 / 状态 / 更新时间 / 查看”表头；桌面宽度下所有任务行严格对齐相同列宽。
5. 任务主标题优先表达动作和对象；次标题显示会话摘要与短编号。长项目名或会话名不能挤掉任务摘要，完整值可通过 tooltip 或详情读取。
6. 任务中心不存在暂停、继续、取消、重跑和任务输入修改控件；只提供查看结果、返回来源和复制诊断。
7. 展开任务后能看到来源、实际模型快照、失败原因、步骤状态、执行次数、检查点和公开事件时间线；模型切换不能改变已有任务的执行信息。
8. 完成任务可查看只读结果、产物目录、模型调用次数、缓存命中和耗时；读取失败不清除任务状态。
9. 任务列表只显示所选范围的数据，定时刷新不会跨项目串数据，也不会覆盖用户当前展开状态。
10. 1440×900、1280×800、1024×680 以及窄屏下无文字、面板和按钮重叠；键盘焦点和减少动态效果符合系统设计标准。
11. 任务中心不显示 API Key、隐藏思维链或未授权正文，不直接修改源文档。
12. 返回来源会话后，运行、暂停和失败任务具有对应的控制入口；页面切换后仍以持久化任务状态恢复控制区。
13. 表中每个 `UI-*` 均关联有效的 `BF-*`、`EP-*`，实现状态与源码和目标态缺口一致。

## 目标态缺口

- 任务展示元数据、模型执行快照和兼容旧任务的序列化迁移。
- 全局任务索引、“全部项目/当前项目”范围选择和跨项目并发状态刷新。
- 统一的 TaskEvent/JobService 状态源和跨会话任务通知。
- 任务事件的分页、筛选和更完整的链路耗时聚合。
- “返回来源消息”精确定位到 `sourceMessageId`；当前先返回来源会话。
- 首次加载骨架屏、局部错误重试和结果预览内联错误，而不是依赖全局错误条。
- 多任务筛选、按状态过滤、搜索和任务归档策略。
- 更完整的证据/缓存可视化，但不暴露内部实现术语或隐藏思维链。
